<%- include('partials/header'); -%>

<h1>Home</h1>
<p> <%=firstParagraph%> </p>

    <% newPosts.forEach(function(element){ %>   
        <h1> <%= element.name.substring(0,100) %> </h1>
        <p> <%= element.body.substring(0,100) + "..." %> <a href="/posts/<%=element._id%>">Read More</a></p> 
    <% }); %>

    

<%- include('partials/footer'); -%>